# Foundation Runtime — Notes

- Sprint renamed from "Bevy Scaffold" — runtime > Cargo.toml
- Single branch replaces bevy-scaffold + environment-loader
- collision.json loaded but not used in gameplay until Sprint 02+
- MainMenu can be minimal (press Enter → Gameplay with map)
- Hot reload: bevy `asset_server` watch mode dev only

## data/maps/barrio_tutorial_01/ (planned)

```
layout.json
collision.json
scene_hooks.json
```
